import { PendingStatus } from "@/components/auth/pending-status";

export const metadata = {
  title: "Pending Verification",
};

export default function PendingPage() {
  return <PendingStatus />;
}
